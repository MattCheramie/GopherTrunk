---
slug: dmr-rs-12-9
title: DMR RS(12,9)
entry_type: algorithm
category: error-correction
description: RS(12,9,4) is the Reed–Solomon code over GF(2^8) that protects DMR Full Link Control — nine data octets plus three parity octets — with each of the three DMR contexts XOR-masking the parity by a fixed seed (0x96 voice LC, 0x99 terminator, 0x6A embedded) that the verifier must undo first.
keywords: DMR RS 12 9, Reed-Solomon 12 9 4, GF(2^8), FLC parity, XOR seed 0x96 0x99 0x6A, generator polynomial, ETSI TS 102 361-1 Annex B.3.12
aka: ["RS(12,9)", "RS129", "Reed-Solomon (12,9,4)"]
autolink: true
infobox:
  - { label: Code, value: "RS(12,9,4) over GF(2^8)" }
  - { label: Field poly, value: "x^8+x^4+x^3+x^2+1 (0x11D)" }
  - { label: Seeds, value: "0x96 / 0x99 / 0x6A per context" }
  - { label: Spec, value: ETSI TS 102 361-1 Annex B.3.12 }
see_also: [reed-solomon-code, dmr-full-link-control, dmr-embedded-lc, bptc, forward-error-correction, cyclic-redundancy-check, dmr]
cite_urls:
  - https://en.wikipedia.org/wiki/Reed%E2%80%93Solomon_error_correction
  - https://en.wikipedia.org/wiki/Finite_field
---

**DMR RS(12,9)** is the **Reed–Solomon(12,9,4)** code over GF(2^8) that DMR layers on top of
[BPTC(196,96)](/reference/bptc/) to protect [Full Link Control](/reference/dmr-full-link-control/).[^rs]
BPTC reconstructs 96 information bits — nine octets of FLC plus three octets of RS parity — and the
RS check tells the receiver whether that reconstruction was actually correct, catching the
systematic mis-decodes that BPTC's own success flag does not.[^ff] The wrinkle that trips up every
first implementation is that DMR XOR-masks the three parity octets with a **context-specific seed**
before transmission, so the verifier has to un-XOR before it can compute syndromes.

<figure class="figure" markdown="0">
<svg viewBox="0 0 470 130" role="img" aria-label="A 12-octet Reed–Solomon codeword: nine data octets holding the Full Link Control followed by three parity octets, with the parity octets XOR-masked by a per-context seed that the verifier removes before checking that the codeword evaluates to zero at three consecutive powers of the field primitive." xmlns="http://www.w3.org/2000/svg">
  <rect x="14" y="30" width="270" height="28" fill="currentColor" fill-opacity="0.10" stroke="currentColor" stroke-width="1.1"/>
  <text x="149" y="48" text-anchor="middle" font-size="8" fill="currentColor">9 data octets · Full Link Control</text>
  <rect x="284" y="30" width="170" height="28" fill="currentColor" fill-opacity="0.24" stroke="currentColor" stroke-width="1.1"/>
  <text x="369" y="48" text-anchor="middle" font-size="8" fill="currentColor">3 parity octets ⊕ seed</text>
  <text x="14" y="80" font-size="8" fill="currentColor">valid codeword: c(α¹) = c(α²) = c(α³) = 0  after un-XOR</text>
  <text x="14" y="98" font-size="8" fill="currentColor">seed: 0x96·voice LC · 0x99·terminator · 0x6A·embedded LC</text>
</svg>
<figcaption>Nine FLC octets and three parity octets form the codeword; the parity is XOR-masked by a per-context seed, and after un-masking a valid codeword evaluates to zero at α¹, α² and α³.</figcaption>
</figure>

## The field and generator

GopherTrunk builds the code in `internal/radio/framing/rs12_9.go`, matching the HBLink `rs129`
reference. The field is GF(2^8) generated by the primitive polynomial
`p(x) = x^8 + x^4 + x^3 + x^2 + 1` (`0x11D`, low byte `0x1D`), with primitive element α = 2. The
generator uses the first-consecutive-root DMR convention:

`g(x) = (x + α¹)(x + α²)(x + α³) = x³ + 14·x² + 56·x + 64`

so a valid codeword satisfies `c(α¹) = c(α²) = c(α³) = 0`. `VerifyRS12_9` evaluates the three
syndromes by forward Horner over the 12 octets and returns true only when all three are zero.

## The context seeds

The one constant a table cannot convey compactly is the per-context XOR seed applied to the parity
octets at transmit time — the verifier must un-XOR them before computing syndromes, and using the
wrong seed rejects every otherwise-valid codeword:

```go
// DMR-specific seeds XOR'd into the 3 parity octets before transmission.
// VerifyRS12_9 un-XORs them before computing syndromes.
var (
    RS129SeedVoiceLCHeader = [3]byte{0x96, 0x96, 0x96}
    RS129SeedTerminatorLC  = [3]byte{0x99, 0x99, 0x99}
    RS129SeedEmbeddedLC    = [3]byte{0x6A, 0x6A, 0x6A}
    RS129SeedNone          = [3]byte{0x00, 0x00, 0x00}
)
```

Each of the three FLC-bearing contexts — the Voice LC Header, the Terminator with LC, and the
[embedded LC](/reference/dmr-embedded-lc/) — masks all three parity octets with its own byte, so
the seed doubles as a lightweight tag: a codeword only verifies against the seed of the context it
came from. `VerifyRS12_9` copies the codeword, un-XORs the parity with the supplied seed, and then
checks the syndromes, leaving the caller's buffer untouched.

## Impl-gap: verify only

This pass implements **verification only**. The code's nominal single-error correction (t = 1) is a
documented follow-up; GopherTrunk leans on the strong [BPTC](/reference/bptc/) / embedded-BPTC layer
for correction over the same bits and uses RS(12,9) as an independent check that catches BPTC
mis-decodes the [CSBK](/reference/csbk/) CRC-16 does not cover. `EncodeRS12_9` is present (for
synthetic streams and round-trip tests) and emits the systematic 12-octet codeword *without* the
seed, which callers XOR in afterward to match on-air framing.

## Relevance to SDR

RS(12,9) is the belt to BPTC's braces on the most valuable DMR PDU — the Full Link Control that
names a call's talkgroup and source radio. Because BPTC can occasionally correct into a valid but
wrong codeword on a marginal signal, an independent RS parity check over the recovered octets is how
GopherTrunk can gain confidence that a decoded FLC is genuine rather than a plausible-looking
artefact, and the seed discipline keeps a Voice LC Header from being mistaken for a Terminator or an
embedded block. Getting the field polynomial, root convention, and the three seeds exactly right is
what makes that check trustworthy.

## Sources

[^rs]: [Reed–Solomon error correction](https://en.wikipedia.org/wiki/Reed%E2%80%93Solomon_error_correction) — Wikipedia, on the code family RS(12,9,4) belongs to.
[^ff]: [Finite field](https://en.wikipedia.org/wiki/Finite_field) — Wikipedia, on the GF(2^8) arithmetic the code is defined over.
